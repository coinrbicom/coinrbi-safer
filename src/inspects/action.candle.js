/*
  @ 
*/
import actions from '../actions/candle.js'
import log from '../services/log/index.js'

Promise.resolve()
  .then(async () => {
    // const data = await actions.getCandles('KRW-BTC', '1', 3600, {}, 'trade_price',  true)
    // log.debug(`Candle Data: ${data.length} items`)
  })

export default {}